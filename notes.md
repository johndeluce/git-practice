## Some Notes from Lab 1.3
**John DeLuce**

1. Make sure to use `git status` often.
2. Before you make any changes, make sure you use `git pull`.
3. Commit changes often using `git commit -m "message"`.
4. Check `git log --oneline` to be able to view previous commits via CLI
